// string datatype
name = "Dijkstra"

// integer
age = 500       // can be written upto 2^56 digits

// boolean
isAlive = true  // unfortunately

// object
const person = new Object();       // OR const person = new object();

person.name = "Dijkstra"
person.age = 30
person.isAlive = false

console.log(person)
// this is simply anything that could be defined

let id = Symbol("id")
console.log(id)

console.log(typeof(Symbol))
